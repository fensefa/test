#include <iostream>
#include <vector>
#include <unordered_map>
#include <string>
using namespace std;

int main() {
    /*
    unordered_map<string, pair<string, pair<float, float> > > test_map;
    test_map.emplace("abc","abc",0.3,0.4);
    */
    vector<pair<int, pair<float, float> > > test_vec;
    test_vec.emplace_back(1,1,1);
}
